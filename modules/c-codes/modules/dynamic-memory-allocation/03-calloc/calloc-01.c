/*
* by Rodrigo Leite
* 10/12/2016
*/

#include <stdio.h>
#include <stdlib.h>

int main()
{
  // Creates an array of 50 integers (4 bytes each).
  int *v = (int*) calloc(50,4);

  // Creates an array of 200 characters "char" (1 byte each)
  char *c = (char*) calloc(200,1);
}
